Run the deterministic `ga-lint` checker for Governed Autonomy `governance.yml` files.

This command is a router to the repository lint CLI, not an LLM review. It reads governance files and reports mechanical errors and warnings. It must not edit files.
