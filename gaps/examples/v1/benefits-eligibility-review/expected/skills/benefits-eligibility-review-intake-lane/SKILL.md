---
name: benefits-eligibility-review-intake-lane
description: Receive and normalize an eligibility request, confirming identity.
process: benefits-eligibility-review
lane: intake_lane
generatedBy: gaps-v1-generator
doNotEditByHand: true
---

# Benefits eligibility review — Intake lane

## Purpose

Receive and normalize an eligibility request, confirming identity.

## Input Quality Gate

Before acting in this lane, confirm every declared evidence input is present with the required fields.

| Evidence id          | Kind                        | Required fields                            |
| -------------------- | --------------------------- | ------------------------------------------ |
| eligibility-request  | triage-observation          | applicantRef, requestedBenefit, receivedAt |
| identity-attestation | regulatory-record-reference | system, recordId, verifiedAt               |

## Rules

**Authority plane:** `data_plane`  
**Autonomy tier:** `draft`  
**Risk tier:** `low`

### Allowed actions

- `draft-artifact` — **Draft a reviewable artifact**. Produce an artifact intended for human review. The artifact is not persisted as approved state until a gate approves it.
- `record-evidence` — **Record evidence**. Write an evidence item to canonical state. Append-only; supersedes via new evidence rather than mutation of prior records.
- `read-external-system-state` — **Read external system state**. Query an external system (issue tracker, identity directory, monitoring dashboard) read-only.
- `request-clarification` — **Request clarification from a participant**. Ask a participant for additional input needed to proceed.

### Prohibited actions

- `approve-own-work` — **Approve own work**. The producer of an artifact also approves it. Always prohibited at any autonomous tier; allowed only when the role explicitly carries both producer and approver decision rights and a separate human attestation records the dual hat.
- `approve-gate` — **Approve a named gate**. Record explicit human approval of a defined gate. Promotes governed transitions on canonical state.
- `make-binding-policy-interpretation` — **Make a binding policy or legal interpretation**. Issue an interpretation of policy, regulation, or law that the organization will treat as authoritative.

## State Loop

### States

| State                   | Label                          | Flags    |
| ----------------------- | ------------------------------ | -------- |
| intake_open             | Open                           | initial  |
| intake_identity_pending | Awaiting identity verification | —        |
| intake_ready            | Ready for evidence gathering   | terminal |
| intake_rejected         | Rejected at intake             | terminal |

### Transitions

| Transition                     | From                    | To                      | Gate | Guard rules                     |
| ------------------------------ | ----------------------- | ----------------------- | ---- | ------------------------------- |
| t-open-to-identity-pending     | intake_open             | intake_identity_pending | —    | defined(eligibility-request)    |
| t-identity-pending-to-ready    | intake_identity_pending | intake_ready            | —    | defined(identity-attestation)   |
| t-identity-pending-to-rejected | intake_identity_pending | intake_rejected         | —    | undefined(identity-attestation) |

## Gates

This lane does not own any gates directly. Refer to upstream lanes for approval gates that apply.

## Evidence To Produce

Produce the following evidence before leaving this lane. Use the spec's `evidenceModel.caseFileItems[]` shape exactly.

| Evidence id         | Kind               | Required fields                            | Retention  |
| ------------------- | ------------------ | ------------------------------------------ | ---------- |
| eligibility-request | triage-observation | applicantRef, requestedBenefit, receivedAt | regulatory |

## Stop Conditions

- Lane reaches terminal state `intake_ready` (Ready for evidence gathering).
- Lane reaches terminal state `intake_rejected` (Rejected at intake).
- Transition `t-open-to-identity-pending` from `intake_open` is guarded; if no rule allows it, the lane stops and escalates.
- Transition `t-identity-pending-to-ready` from `intake_identity_pending` is guarded; if no rule allows it, the lane stops and escalates.
- Transition `t-identity-pending-to-rejected` from `intake_identity_pending` is guarded; if no rule allows it, the lane stops and escalates.
- Gate `escalated_review_gate` is escalating; reaching it pauses the lane until human approval.

## Non-claim language

This skill is a generated artifact of a GAPS v1 process specification. It does not by itself constitute regulatory compliance, certification, legal sufficiency, or runtime execution. The spec is the source of truth; do not edit this file by hand — regenerate from the spec instead.
